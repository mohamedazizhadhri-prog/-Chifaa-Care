┌─────────────────────────────────────────────────────────────────┐
│                    ChifaaCare Startup Flow                      │
└─────────────────────────────────────────────────────────────────┘

                    FIRST TIME ONLY
    ┌──────────────────────────────────────────────┐
    │  1. Run: setup-database.bat                  │
    │     OR                                       │
    │     npm run setup                            │
    └──────────────┬───────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────────────┐
    │  What happens:                               │
    │  ✓ Install frontend dependencies            │
    │  ✓ Install backend dependencies              │
    │  ✓ Connect to Neon Database                  │
    │  ✓ Generate Prisma Client                    │
    │  ✓ Run Database Migrations                   │
    │  ✓ Create all tables                         │
    └──────────────┬───────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────────────┐
    │  ✅ Setup Complete!                          │
    └──────────────────────────────────────────────┘


                    EVERY TIME
    ┌──────────────────────────────────────────────┐
    │  2. Run: start-all.bat                       │
    │     OR manually start both servers           │
    └──────────────┬───────────────────────────────┘
                   │
                   ├─────────────────┬─────────────┐
                   ▼                 ▼             ▼
    ┌─────────────────────┐  ┌─────────────────────┐
    │   Backend Server    │  │  Frontend Server    │
    │   Port: 3000        │  │  Port: 4200         │
    │   ────────────      │  │  ────────────       │
    │   Express.js        │  │  Angular 17         │
    │   Socket.io         │  │  Components         │
    │   API Routes        │  │  Services           │
    │   Authentication    │  │  UI/UX              │
    └──────────┬──────────┘  └──────────┬──────────┘
               │                        │
               └────────────┬───────────┘
                           ▼
            ┌────────────────────────────┐
            │   Neon PostgreSQL Cloud    │
            │   ──────────────────────   │
            │   • Users & Auth           │
            │   • Patients & Doctors     │
            │   • Appointments           │
            │   • Medical Records        │
            │   • Messages               │
            │   • Audit Logs             │
            └────────────────────────────┘


                    ACCESS POINTS
    ┌──────────────────────────────────────────────┐
    │  Frontend:  http://localhost:4200            │
    │  Backend:   http://localhost:3000            │
    │  API Docs:  http://localhost:3000/api-docs   │
    │  DB GUI:    npm run db:studio                │
    └──────────────────────────────────────────────┘


                 PROJECT STRUCTURE
    ┌──────────────────────────────────────────────┐
    │  ChifaaCare-samedatabase/                    │
    │  ├── src/                  ← Frontend code   │
    │  │   ├── app/                                │
    │  │   │   ├── components/                     │
    │  │   │   ├── services/                       │
    │  │   │   ├── models/                         │
    │  │   │   └── portals/                        │
    │  │   └── assets/                             │
    │  │                                           │
    │  ├── chifaacare-backend/   ← Backend code    │
    │  │   ├── src/                                │
    │  │   │   ├── auth/                           │
    │  │   │   ├── controllers/                    │
    │  │   │   ├── routes/                         │
    │  │   │   └── modules/                        │
    │  │   ├── prisma/                             │
    │  │   │   └── schema.prisma  ← DB Schema      │
    │  │   └── .env             ← Configuration    │
    │  │                                           │
    │  ├── start-all.bat         ← START HERE!    │
    │  ├── setup-database.bat    ← Setup script   │
    │  ├── check-system.bat      ← Health check   │
    │  ├── QUICK_START.md        ← Read this!     │
    │  └── START_PROJECT.md      ← Full guide     │
    └──────────────────────────────────────────────┘


              TROUBLESHOOTING FLOW
    ┌──────────────────────────────────────────────┐
    │  Something not working?                      │
    └──────────────┬───────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────────────┐
    │  Step 1: Run check-system.bat                │
    │  → Shows what's missing/broken               │
    └──────────────┬───────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────────────┐
    │  Step 2: Read error messages                 │
    │  → Terminal shows specific errors            │
    └──────────────┬───────────────────────────────┘
                   │
                   ▼
    ┌──────────────────────────────────────────────┐
    │  Step 3: Common fixes                        │
    │  • Port in use → Kill process                │
    │  • DB error → Check .env DATABASE_URL        │
    │  • Dependencies → npm install                │
    │  • Prisma → npm run prisma:generate          │
    └──────────────────────────────────────────────┘


                DATA FLOW DIAGRAM
    
    User Browser (localhost:4200)
           │
           │ HTTP Requests
           ▼
    Angular Frontend
           │
           │ API Calls (REST)
           │ WebSocket (Socket.io)
           ▼
    Backend API (localhost:3000)
           │
           ├─→ Authentication (JWT)
           │
           ├─→ Business Logic
           │   (Controllers, Services)
           │
           └─→ Prisma ORM
               │
               │ SQL Queries
               ▼
    Neon PostgreSQL Database
    (Cloud-hosted in EU)


             SECURITY LAYERS
    ┌──────────────────────────────────────────────┐
    │  1. HTTPS/TLS (Production)                   │
    │  2. JWT Authentication                       │
    │  3. Role-Based Access Control (RBAC)         │
    │  4. Password Hashing (bcrypt)                │
    │  5. SQL Injection Protection (Prisma)        │
    │  6. CORS Configuration                       │
    │  7. Audit Logging                            │
    │  8. Data Encryption (HIPAA/GDPR)             │
    └──────────────────────────────────────────────┘


          DEVELOPMENT WORKFLOW
    ┌──────────────────────────────────────────────┐
    │                                              │
    │  Morning:                                    │
    │  1. Pull latest code (git pull)              │
    │  2. Start servers (start-all.bat)            │
    │  3. Open http://localhost:4200               │
    │                                              │
    │  During Development:                         │
    │  • Frontend auto-reloads on save             │
    │  • Backend auto-restarts (ts-node-dev)       │
    │  • Check terminal for errors                 │
    │  • Use browser DevTools (F12)                │
    │                                              │
    │  Before Committing:                          │
    │  1. Test changes                             │
    │  2. Check for console errors                 │
    │  3. Run: npm run build                       │
    │  4. Git commit & push                        │
    │                                              │
    │  Evening:                                    │
    │  • Close terminal windows (Ctrl+C)           │
    │  • Or just close the windows                 │
    │                                              │
    └──────────────────────────────────────────────┘


              DATABASE TABLES
    ┌──────────────────────────────────────────────┐
    │  Core Tables:                                │
    │  • User (patients, doctors, admins)          │
    │  • DoctorProfile                             │
    │  • PatientProfile                            │
    │  • Appointment                               │
    │  • Message                                   │
    │  • TreatmentPlan                             │
    │  • Medication                                │
    │  • MedicalHistory                            │
    │                                              │
    │  Clinic Management:                          │
    │  • Clinic                                    │
    │  • ClinicService                             │
    │  • Document                                  │
    │                                              │
    │  Security & Compliance:                      │
    │  • Role                                      │
    │  • Permission                                │
    │  • UserRole                                  │
    │  • RolePermission                            │
    │  • AuditLog                                  │
    │                                              │
    │  Communication:                              │
    │  • CallLog                                   │
    │  • Notification                              │
    │  • PushSubscription                          │
    └──────────────────────────────────────────────┘


          KEY FEATURES IMPLEMENTED
    ┌──────────────────────────────────────────────┐
    │  ✓ User Authentication (JWT + Auth0)         │
    │  ✓ Patient Management                        │
    │  ✓ Doctor Profiles                           │
    │  ✓ Appointment Scheduling                    │
    │  ✓ Telemedicine (Video/Voice calls)          │
    │  ✓ Messaging System                          │
    │  ✓ Treatment Plans                           │
    │  ✓ Medication Tracking                       │
    │  ✓ Medical History                           │
    │  ✓ Clinic Onboarding                         │
    │  ✓ Document Management                       │
    │  ✓ Audit Logging                             │
    │  ✓ Push Notifications                        │
    │  ✓ Role-Based Access Control                 │
    │  ✓ Multi-language Support                    │
    │  ✓ PWA (Offline Support)                     │
    └──────────────────────────────────────────────┘
